{
  "name": "tmt-official-admin",
  "version": "1.0.0",
  "private": true,
  "description": "TMT OFFICIAL — Admin Panel (CMS) — Vercel Serverless + Supabase",
  "engines": {
    "node": ">=18.x"
  },
  "scripts": {
    "dev": "vercel dev",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.45.4",
    "resend": "^4.0.0"
  }
}
